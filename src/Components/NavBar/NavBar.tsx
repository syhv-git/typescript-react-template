import {AppBar, Button} from "@mui/material";

const pages = ["Home", "Products", "FAQs"];
const settings = ["Mode"];

function NavBar() {
  return (
    <div id="nav">
      <AppBar>
      </AppBar>
    </div>
  );
}

export default NavBar;
